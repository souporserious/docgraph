{
  "name": "@docgraph/headings",
  "version": "0.0.1",
  "description": "Parse headings from MDX or Markdown strings.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
