import Guidelines from "./guidelines.mdx"

`Box` is the base component that other components can be built from. It is responsible for converting style props into CSS rules.

```tsx
import { Box, Stack } from "components"

export function Basic({ name = "Box" }) {
  return (
    <Stack width="100%" height="100%" alignItems="center" justifyContent="center">
      <Box padding={16} backgroundColor="tomato" color="white">
        Hello {name}
      </Box>
    </Stack>
  )
}

export function Usage() {
  return <Example name="Basic" />
}
```

<Guidelines />

```tsx
import { Box } from "components"

export function Example({ name = "Box" }) {
  return <Box>Hello {name}</Box>
}
```
